현재 폴더는 비어 있다. 이 폴더에 Flutter 기반 Android 애플리케이션을 처음부터 생성하고 구현한다.

## 1. 프로젝트 목표

삼성 노트처럼 일반 메모와 손글씨 필기를 제공하면서, PDF와 HWP/HWPX 문서를 불러와 열람하고 문서 위에 필기할 수 있는 통합 문서 노트 앱을 만든다.

Google Play Store 출시를 목표로 하며, 첫 번째 버전은 기능을 과도하게 확장하지 않고 실제로 사용할 수 있는 MVP 완성을 우선한다.

앱의 핵심 콘셉트는 다음과 같다.

> 메모를 작성하고, PDF와 한글 문서를 불러와 필기·검토·관리할 수 있는 한국형 통합 문서 노트 앱

PDF 앱과 HWP 앱을 별도로 만들지 않는다. 하나의 앱에서 메모, PDF, HWP/HWPX 문서를 통합 관리한다.

## 2. 기본 프로젝트 정보

* 프레임워크: Flutter 최신 stable
* 지원 플랫폼: Android 우선
* 앱 이름: DocNote
* 패키지명: com.bangceps.docnote
* 최소 Android 버전: 실제 사용 라이브러리와 호환되는 범위에서 합리적으로 설정
* 화면 방향: 스마트폰 세로 모드 우선
* 태블릿 화면도 깨지지 않도록 반응형 구성
* 언어: 한국어
* 테마: 밝은 테마 우선
* 상태 관리: Riverpod
* 라우팅: go_router
* 로컬 데이터베이스: Drift 또는 Isar 중 프로젝트 목적에 더 적합한 것을 선정
* 파일 선택: Android Storage Access Framework와 호환되는 안정적인 Flutter 패키지 사용
* 파일 저장: 앱 전용 저장소와 사용자가 선택한 외부 위치를 구분
* 아키텍처: feature-first 구조와 repository 패턴 적용

패키지를 선택할 때는 현재 유지보수 상태, Android 최신 버전 호환성, 라이선스를 확인한다. 오래되었거나 유지보수가 중단된 패키지는 사용하지 않는다.

## 3. MVP 구현 범위

### 3.1 홈 화면

홈 화면에는 다음 항목을 제공한다.

* 최근 문서
* 즐겨찾기
* 최근 메모
* 빠른 메모 생성
* PDF 가져오기
* HWP/HWPX 가져오기
* 전체 문서 보기

홈 화면은 카드만 나열된 단조로운 구조를 피한다. 최근 문서를 중심으로 정돈된 생산성 앱 형태로 구성한다.

### 3.2 하단 내비게이션

다음 메뉴를 제공한다.

* 홈
* 문서
* 새로 만들기
* 검색
* 설정

가운데의 새로 만들기 버튼은 다른 메뉴보다 명확하게 강조한다.

새로 만들기를 누르면 바텀시트로 다음 메뉴를 표시한다.

* 새 메모
* 필기 노트
* PDF 가져오기
* HWP/HWPX 가져오기

### 3.3 일반 메모

일반 메모는 다음 기능을 제공한다.

* 제목 입력
* 본문 텍스트 입력
* 체크리스트
* 이미지 첨부
* 자동 저장
* 수정일 표시
* 즐겨찾기
* 폴더 지정
* 삭제 및 휴지통 이동

첫 번째 버전에서는 복잡한 블록 에디터보다 안정적인 텍스트 메모 작성 경험을 우선한다.

### 3.4 필기 노트

빈 페이지에 직접 필기하는 노트를 제공한다.

필수 기능은 다음과 같다.

* 펜
* 형광펜
* 지우개
* 실행 취소
* 다시 실행
* 펜 색상 선택
* 펜 굵기 선택
* 페이지 추가
* 페이지 삭제
* 페이지 이동
* 확대 및 축소
* 자동 저장

삼성 S펜과 일반 터치 입력을 모두 지원한다.

손가락 스크롤과 펜 필기가 충돌하지 않도록 입력 모드를 설계한다.

필기 데이터는 단순 이미지로만 저장하지 않는다. 획 단위 벡터 데이터로 저장해 다시 편집할 수 있도록 구성한다.

각 획에는 최소한 다음 정보를 저장한다.

* 문서 ID
* 페이지 ID
* 도구 종류
* 좌표 목록
* 압력값
* 색상
* 굵기
* 투명도
* 생성 순서

좌표는 화면의 실제 픽셀이 아니라 페이지 크기를 기준으로 정규화해 저장한다. 화면 크기와 확대 비율이 달라져도 동일한 위치에 렌더링되어야 한다.

### 3.5 PDF 문서

PDF 파일을 가져오고 문서 위에 필기할 수 있도록 구현한다.

필수 기능은 다음과 같다.

* PDF 파일 선택
* PDF 페이지 렌더링
* 페이지 스크롤
* 페이지 썸네일
* 현재 페이지 표시
* 확대 및 축소
* 펜 필기
* 형광펜
* 지우개
* 실행 취소 및 다시 실행
* 문서 검색
* 즐겨찾기
* 문서 이름 변경
* 원본 파일 열기
* 필기 포함 PDF 내보내기

PDF 원본 데이터와 필기 데이터는 분리해서 보관한다.

사용자가 직접 내보내기를 실행하기 전까지 원본 PDF를 덮어쓰지 않는다.

PDF 내보내기 기능은 필기 레이어가 각 페이지의 정확한 위치에 합성되도록 구현한다.

첫 번째 버전에서 PDF 내부의 기존 텍스트를 직접 수정하는 기능은 구현하지 않는다.

### 3.6 HWP/HWPX 문서

rhwp를 이용해 HWP와 HWPX 문서를 처리한다.

작업을 시작하기 전에 현재 사용 가능한 rhwp 저장소, 패키지, 라이선스, Flutter 및 Android 연동 방법을 확인한다.

검증되지 않은 API를 추측해서 작성하지 않는다. 실제 존재하는 라이브러리와 API만 사용한다.

rhwp가 Flutter에서 직접 사용하기 어려운 경우 다음 대안을 순서대로 검토한다.

1. Flutter용 rhwp 래퍼
2. Rust FFI 연동
3. WebAssembly 및 WebView 연동
4. 별도 네이티브 Android 모듈 연동

선정한 방식과 이유를 README에 기록한다.

첫 번째 버전의 HWP/HWPX 기능 범위는 다음과 같다.

* HWP/HWPX 파일 선택
* 문서 열기
* 페이지 단위 렌더링
* 페이지 이동
* 확대 및 축소
* 페이지 위 펜 필기
* 형광펜
* 지우개
* 주석 데이터 자동 저장
* PDF로 내보내기
* 원본 파일 보존

첫 번째 버전에서는 다음 기능을 구현하지 않는다.

* HWP 본문 직접 수정
* HWP 표 구조 수정
* HWP 서식 완전 편집
* HWP 원본 덮어쓰기
* 한컴오피스와 100% 동일한 렌더링 보장

rhwp 연동이 즉시 완료되지 않더라도 앱 전체 개발을 중단하지 않는다.

HWP 엔진은 인터페이스로 추상화하고, 초기에는 샘플 또는 플레이스홀더 렌더러로 앱 흐름을 완성한 뒤 실제 rhwp 구현체를 연결할 수 있도록 구성한다.

예시는 다음 책임을 가진 인터페이스로 설계한다.

* 문서 열기
* 문서 메타데이터 읽기
* 페이지 수 조회
* 페이지 렌더링
* 텍스트 추출
* PDF 변환
* 리소스 해제

HWP 기능이 지원되지 않는 환경에서는 앱이 종료되지 않도록 사용자에게 명확한 오류 메시지를 표시한다.

## 4. 문서 통합 모델

메모, 필기 노트, PDF, HWP, HWPX를 공통 문서 모델로 관리한다.

문서 모델에는 최소한 다음 정보가 포함되어야 한다.

* ID
* 제목
* 문서 유형
* 원본 파일 경로
* 내부 작업 파일 경로
* 썸네일 경로
* 생성일
* 수정일
* 마지막으로 열어본 날짜
* 즐겨찾기 여부
* 휴지통 여부
* 폴더 ID
* 페이지 수

문서 유형은 다음과 같이 분리한다.

* textNote
* drawingNote
* pdf
* hwp
* hwpx

PDF와 HWP 문서에서 사용하는 필기 레이어는 동일한 데이터 구조를 재사용한다.

## 5. 권장 폴더 구조

다음과 유사한 feature-first 구조로 구성한다.

```text
lib/
├─ app/
│  ├─ app.dart
│  ├─ router.dart
│  └─ theme/
├─ core/
│  ├─ database/
│  ├─ file_storage/
│  ├─ error/
│  ├─ utils/
│  └─ widgets/
├─ features/
│  ├─ home/
│  ├─ documents/
│  ├─ notes/
│  ├─ drawing/
│  ├─ pdf/
│  ├─ hwp/
│  ├─ search/
│  ├─ folders/
│  ├─ trash/
│  └─ settings/
└─ main.dart
```

각 기능 폴더는 필요에 따라 다음 계층으로 나눈다.

```text
data/
domain/
presentation/
```

과도하게 복잡한 추상화는 피하지만 PDF, HWP, 필기 엔진은 서로 독립적으로 교체할 수 있어야 한다.

## 6. UI 및 UX 방향

전체 디자인은 최신 Android 생산성 앱과 태블릿 노트 앱을 참고하되 특정 앱을 그대로 복제하지 않는다.

다음 원칙을 적용한다.

* Material 3 사용
* 완전한 흰색 배경에 카드만 반복하는 구조를 피함
* 연한 중립 배경색과 명확한 콘텐츠 계층 사용
* 둥근 모서리를 과도하게 사용하지 않음
* 주요 작업 버튼을 명확하게 배치
* 문서 유형은 색상보다 아이콘과 라벨로 구분
* 빈 화면에는 사용자가 다음 행동을 이해할 수 있는 안내 표시
* 모든 주요 작업에 로딩, 성공, 실패 상태 제공
* 삭제와 덮어쓰기는 확인 절차 제공
* 자동 저장 상태를 상단에 표시
* Android 뒤로 가기 동작을 자연스럽게 처리
* 휴대전화와 태블릿 레이아웃을 분리 대응

문서 편집 화면은 다음과 같이 구성한다.

* 상단: 뒤로 가기, 문서 제목, 자동 저장 상태, 더보기
* 중앙: 문서 또는 필기 캔버스
* 하단: 펜, 형광펜, 지우개, 선택, 실행 취소, 다시 실행
* 태블릿: 도구 모음을 좌측 또는 우측에 세로로 표시할 수 있도록 반응형 적용

## 7. 데이터 및 파일 저장 정책

원본 파일을 절대 자동으로 덮어쓰지 않는다.

파일은 다음처럼 분리해서 관리한다.

```text
documents/{documentId}/
├─ original/
├─ working/
├─ annotations/
├─ thumbnails/
├─ exports/
└─ metadata/
```

Android 파일 접근 권한은 최소한으로 요청한다.

가능하면 Storage Access Framework를 사용하고, 불필요한 전체 파일 접근 권한은 사용하지 않는다.

사용자가 원본 파일을 삭제하거나 이동한 경우에도 앱이 비정상 종료되지 않도록 처리한다.

데이터베이스와 실제 파일 간 불일치를 감지하고 복구할 수 있는 검증 로직을 둔다.

## 8. 자동 저장과 복구

편집 중인 데이터는 일정 시간의 debounce를 적용해 자동 저장한다.

다음 상황에서도 최대한 복구할 수 있어야 한다.

* 앱 강제 종료
* Android 프로세스 종료
* 화면 회전
* 다른 앱으로 전환
* 저장 공간 부족
* 파일 접근 권한 해제

자동 저장 실패 시 조용히 무시하지 말고 사용자에게 상태를 표시한다.

## 9. 검색

MVP 검색 범위는 다음과 같다.

* 문서 제목
* 메모 본문
* 문서 유형
* 폴더
* 최근 수정일

PDF 또는 HWP 본문 검색은 라이브러리가 안정적으로 텍스트를 추출할 수 있을 때만 지원한다.

지원하지 않는 경우 검색 가능하다고 표시하지 않는다.

## 10. 설정 화면

다음 설정을 제공한다.

* 테마 설정
* 기본 펜 색상
* 기본 펜 굵기
* 자동 저장 설정
* 저장 공간 사용량 확인
* 휴지통 비우기
* 개인정보처리방침
* 오픈소스 라이선스
* 앱 버전
* 데이터 내보내기
* 데이터 초기화

## 11. 예외 처리

다음 상황을 반드시 처리한다.

* 비밀번호가 설정된 PDF
* 손상된 PDF
* 손상된 HWP/HWPX
* 지원하지 않는 HWP 기능
* 파일 용량이 지나치게 큰 경우
* 페이지 수가 많은 문서
* 메모리 부족
* 저장 공간 부족
* 파일 접근 권한 취소
* 원본 파일 삭제
* 렌더링 실패
* PDF 내보내기 실패

예외 발생 시 앱이 종료되지 않도록 하고, 사용자에게 이해하기 쉬운 한국어 메시지를 표시한다.

개발 로그에는 원인을 확인할 수 있는 기술 정보를 남기되 사용자 화면에는 스택 트레이스를 노출하지 않는다.

## 12. 성능 요구사항

* 긴 문서를 한 번에 모두 렌더링하지 않는다.
* 현재 페이지 주변만 지연 렌더링한다.
* 페이지 썸네일을 캐시한다.
* 원본 문서와 렌더링 캐시를 구분한다.
* 메모리가 부족하면 오래된 페이지 캐시를 해제한다.
* 필기 입력 중 프레임 드롭을 최소화한다.
* 대용량 파일 작업은 UI 스레드를 차단하지 않는다.
* 문서 목록의 썸네일은 비동기로 불러온다.

## 13. 테스트

다음 테스트를 작성한다.

### 단위 테스트

* 문서 생성
* 문서 이름 변경
* 문서 삭제와 복원
* 즐겨찾기 변경
* 필기 좌표 정규화와 복원
* 파일 저장 경로 생성
* 자동 저장 로직
* 문서 유형 판별

### 위젯 테스트

* 홈 화면
* 문서 목록
* 새로 만들기 바텀시트
* 메모 편집
* 빈 화면
* 오류 화면

### 통합 테스트

* 새 메모 작성 후 앱 재실행
* PDF 가져오기
* PDF 위 필기 후 재실행
* 문서 삭제 후 복원
* 지원하지 않는 파일 열기
* 권한이 취소된 파일 열기

실제 PDF와 HWP/HWPX 테스트 파일은 저장소에 포함 가능한 라이선스의 샘플만 사용한다.

## 14. Google Play 출시 준비

다음 사항을 고려해 프로젝트를 구성한다.

* Android App Bundle 빌드 가능
* release 빌드 설정
* 난독화 적용 가능 구조
* targetSdk 최신 정책 대응
* 64비트 지원
* 앱 아이콘 임시 리소스
* 스플래시 화면
* 개인정보처리방침 연결 위치
* 데이터 안전 섹션 작성에 필요한 데이터 처리 내역 문서화
* 불필요한 권한 제거
* 광고 및 분석 SDK는 초기 버전에 넣지 않음
* 사용자 파일을 외부 서버로 전송하지 않음
* 모든 문서 처리는 기본적으로 기기 내부에서 수행

## 15. README 작성

README.md에 다음 내용을 작성한다.

* 앱 소개
* 주요 기능
* 프로젝트 구조
* 실행 방법
* Android 빌드 방법
* 사용한 주요 패키지
* 각 패키지의 역할
* PDF 처리 방식
* HWP/rhwp 연동 방식
* 필기 데이터 저장 방식
* 현재 지원 기능
* 아직 지원하지 않는 기능
* 알려진 문제
* 테스트 실행 방법
* release AAB 생성 방법

rhwp 연동이 미완성이라면 미완성 사실과 이유, 후속 구현 방법을 명확히 작성한다.

## 16. 작업 방식

다음 순서로 작업한다.

1. 현재 폴더 상태 확인
2. Flutter 개발 환경 확인
3. Flutter 프로젝트 생성
4. 전체 아키텍처 설계
5. 필요한 패키지 선정
6. 데이터 모델 및 로컬 데이터베이스 구현
7. 홈과 문서 관리 기능 구현
8. 일반 메모 구현
9. 필기 엔진 구현
10. PDF 가져오기와 필기 구현
11. HWP 엔진 인터페이스 구현
12. rhwp 연동 가능성 검증 및 구현
13. 검색과 설정 구현
14. 예외 처리
15. 테스트 작성
16. 정적 분석 및 포맷팅
17. Android debug APK 빌드
18. Android release AAB 빌드 가능 여부 확인
19. README와 작업 결과 작성

계획만 작성하고 중단하지 않는다. 실제 파일을 생성하고 실행 가능한 코드를 구현한다.

각 단계에서 문제가 발생하면 우회 가능한 기능까지 모두 완성한 뒤, 해결하지 못한 부분만 명확하게 정리한다.

## 17. 완료 조건

다음 조건을 만족해야 작업이 완료된 것으로 본다.

* Flutter 프로젝트가 정상 생성되어 있음
* 앱이 Android에서 실행됨
* 홈 화면과 하단 내비게이션이 동작함
* 일반 메모를 생성·수정·삭제할 수 있음
* 필기 노트를 생성하고 필기 데이터를 저장할 수 있음
* PDF를 가져오고 문서 위에 필기할 수 있음
* 앱 재실행 후 필기와 문서가 복원됨
* HWP/HWPX 엔진 구조가 분리되어 있음
* 가능한 범위에서 rhwp가 실제 연동되어 있음
* rhwp 연동이 불가능한 경우에도 앱 전체가 정상 실행됨
* 원본 문서를 자동으로 덮어쓰지 않음
* 주요 오류에 대한 사용자 안내가 있음
* flutter analyze가 통과함
* 테스트가 통과함
* debug APK 빌드가 성공함
* README가 작성되어 있음

## 18. 최종 보고

작업 완료 후 다음 내용을 정리한다.

* 생성한 프로젝트 구조
* 구현한 기능
* 사용한 패키지와 선정 이유
* rhwp 연동 결과
* PDF 구현 방식
* 필기 저장 방식
* 실행한 테스트
* 빌드 결과
* 아직 구현하지 못한 기능
* 알려진 문제
* 다음 개발 우선순위
* 실행 및 빌드 명령어

중요한 구현을 임시 코드나 TODO만 남긴 상태로 완료 처리하지 않는다. 다만 HWP 완전 편집처럼 MVP 범위에서 제외한 기능은 억지로 구현하지 않는다.
