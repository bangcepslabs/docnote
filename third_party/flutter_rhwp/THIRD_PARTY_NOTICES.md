# Third-Party Notices

This package is MIT licensed. It bundles or depends on the following
third-party projects as part of the Flutter/Rust plugin build.

## Bundled Source

### rhwp

- Project: `rhwp`
- Upstream: https://github.com/edwardkim/rhwp
- Version pinned in this package: v0.7.12
- Location: `rust/vendor/rhwp`
- License: MIT
- License file: `rust/vendor/rhwp/LICENSE`
- Upstream third-party notices: `rust/vendor/rhwp/THIRD_PARTY_LICENSES.md`

`rust/vendor/rhwp` is intentionally committed so plugin builds do not fetch the
Rust core from GitHub at build time.

### Cargokit

- Project: Cargokit integration scripts for Flutter Rust builds
- Location: `cargokit`
- License: MIT and Apache-2.0
- License file: `cargokit/LICENSE`

The Cargokit files are used to build and package the Rust static or dynamic
library for Flutter platforms.

## Direct Dart Dependencies

The direct Dart package dependencies are resolved through `pubspec.lock`.
Their license files are provided by the packages downloaded from pub.dev.

| Package | Resolved version | License | Purpose |
| --- | --- | --- | --- |
| `flutter_rust_bridge` | 2.12.0 | MIT | Dart/Rust bridge runtime and generated binding support |
| `flutter_svg` | 2.3.0 | MIT | SVG page rendering in `RhwpViewer` |
| `web` | 1.1.1 | BSD-style; generated web API data includes Mozilla attribution under CC-BY-SA 2.5 where noted upstream | Web editor host and browser interop |
| `webview_all` | 1.0.3 | BSD-3-Clause | Native full editor WebView host for Android, iOS, macOS, Windows, and Linux |

Flutter SDK packages are governed by the Flutter/Dart SDK licenses.

`webview_all` resolves platform implementation packages including
`webview_all_linux`, `webview_all_windows`, `webview_all_web`,
`webview_flutter_android`, `webview_flutter_platform_interface`, and
`webview_flutter_wkwebview` through `pubspec.lock`.

## Direct Rust Dependencies

The Rust facade crate depends on:

- `rhwp_core`, provided by the vendored `rust/vendor/rhwp` source.
- `flutter_rust_bridge`
- `serde`
- `serde_json`
- `zip`

The full Rust dependency graph is locked in `rust/Cargo.lock`. The vendored
`rhwp` third-party notice file contains its upstream Rust dependency license
summary.

## Web Editor Module

`RhwpWebEditor` can load upstream `@rhwp/editor` from a configurable ESM URL.
The example defaults to `https://esm.sh/@rhwp/editor`, but that module is not
vendored into this package. Production apps that self-host or bundle
`@rhwp/editor` should include the notices required by that bundled npm package
and its dependency graph.

## Generated Files

Files under `lib/src/rust/frb_generated*.dart` and `rust/src/frb_generated.rs`
are generated by `flutter_rust_bridge` for this package. They are distributed
with this package under the package license, while the generator/runtime remain
subject to the `flutter_rust_bridge` license noted above.
